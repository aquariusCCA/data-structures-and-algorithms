# 概述

> 所属章节：[二、基础数据结构](../README.md) / [链表](./README.md)  
> 关键字：链表、节点、单向链表、双向链表、循环链表、哨兵节点、时间复杂度  
> 建議回查情境：忘记链表定义、分类、随机访问性能或插入删除复杂度时

## 本节导读

這一節先建立鏈表的基本模型：元素不要求連續存儲，而是透過節點引用串起順序。第一次閱讀時建議先看鏈表分類，再用複雜度對比數組的隨機訪問與鏈表的插入刪除。

## 你會在這篇學到什麼

- 鏈表的基本定義與存儲特性
- 單向、雙向、循環鏈表與哨兵節點的差異
- 鏈表查找、插入、刪除的時間複雜度

---

### 定義

在计算机科学中，链表是数据元素的线性集合，其每个元素都指向下一个元素，元素存储上并不连续

> In computer science, a linked list is a linear collection of data elements whose order is not given by their physical placement in memory. Instead, each element points to the next.
>

可以分类为

- 单向链表，每个元素只知道其下一个元素是谁
  - ![](../images/1772278398931_XuQm97PpGh.png)
- 双向链表，每个元素知道其上一个元素和下一个元素
  - ![](../images/1772278398931_Vnmsc7IuWd.png)
- 循环链表，通常的链表尾节点 tail 指向的都是 null，而循环链表的 tail 指向的是头节点 head
  - ![](../images/1772289557352_FA5oz2ioAH.png)
- 链表内还有一种特殊的节点称为哨兵（Sentinel）节点，也叫做哑元（ Dummy）节点，它不存储数据，通常用作头尾，用来简化边界判断，如下图所示
  - ![](../images/1772289557362_ea8VuioaxT.png)

### 随机访问性能

根据 index 查找，时间复杂度 $O(n)$

### 插入或删除性能

- 起始位置：$O(1)$
- 结束位置：如果已知 tail 尾节点是 $O(1)$，不知道 tail 尾节点是 $O(n)$
- 中间位置：根据 index 查找时间 $+ O(1)$

---

## 導航

- 上一篇：[數組](../1%20數組/README.md)
- 返回：[链表入口](./README.md)
- 下一篇：[单向链表](./02%20单向链表.md)
