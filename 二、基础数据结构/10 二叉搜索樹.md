# 二叉搜索樹（Binary Search Tree, BST）概述

在前面，我們已經學過一些基本的資料結構。
不過，如果我們想要在這些資料結構中**快速查找某個元素**，效率通常不算高。

這時候，很多人會想到我們之前學過的 **二分查找**。

## 為什麼不能只用二分查找？

二分查找的確可以大幅提升查詢效率，但它有一個重要前提：

> **必須建立在「已排序的陣列」上。**

問題是，若原本的資料沒有排序，我們就得先花時間排序。
而排序本身是有成本的，有時候會變成：

* 還沒開始查找
* 就先花了不少時間在排序上

這樣一來，整體效率未必划算。

## 有沒有更好的折衷方法？

有。
我們可以改用另一種資料結構：**二叉搜索樹**。

二叉搜索樹的好處是：

* 可以提升查找效率
* 在插入元素的過程中，就自然維持一定的大小順序

也就是說，它不像陣列那樣需要先整體排序，而是在加入資料時，就把元素放到合適的位置。

## 什麼是二叉搜索樹？

下面這棵樹就是一棵二叉搜索樹，也叫做 **二叉排序樹**。

```text
              4
           /     \
          2       7
         / \      / \
        1   3    6   8
                /
               5 
```

## 二叉搜索樹的兩個核心特徵

要成為二叉搜索樹，必須滿足以下條件：

### 1. 每個節點都有一個 key，用來比較大小

* `key` 可以理解成節點的值
* `key` **不能重複**

上圖中的 `1、2、3、4、5、6、7、8`，就是各節點的 key。

### 2. 任意節點都要符合大小規則

對於每一個節點來說：

* **左子樹所有節點的 key，都比它小**
* **右子樹所有節點的 key，都比它大**

這是二叉搜索樹最重要的性質。

### 例子：為什麼 6 的右孩子不能亂放？

假設我們想幫節點 `6` 加上一個右孩子，那麼這個值必須同時滿足：

* 比 `6` 大
* 又要比 `7` 小

所以像 `6.5` 就是合理的。

```text
              4
           /     \
          2       7
         / \      / \
        1   3    6   8
                / \
               5   6.5
```

這樣整棵樹依然符合二叉搜索樹的規則。

## 二叉搜索樹為什麼查找比較快？

假設我們要查找 `8`。

### 查找過程

1. 從根節點 `4` 開始
   因為 `8 > 4`，所以往右子樹走

2. 到節點 `7`
   因為 `8 > 7`，所以繼續往右子樹走

3. 到節點 `8`
   找到了目標

總共只比較了 **3 次**。

## 查找效率和什麼有關？

二叉搜索樹的查找效率，通常和 **樹的高度** 有關。

樹越矮、越平衡，查找通常越快。
因為每次比較後，都可以排除一大半不可能的範圍。

所以在理想情況下，二叉搜索樹的查找效率會很好。

## 但它也有缺點：可能會退化

如果樹長得很不平均，例如下面這樣：

```text
                5
               /
              4
             /
            3
           /
          2
         /
        1 
```

這棵樹幾乎已經不像樹了，而是很像一條鏈。

這種情況代表：

* 左右子樹高度差很多
* 樹**不平衡**

### 不平衡時，查找效率會變差

例如要查找 `1`：

1. 先比 `5`
2. 再比 `4`
3. 再比 `3`
4. 再比 `2`
5. 最後比 `1`

總共要比較 **5 次**。

這時候查找效率就退化成和線性查找差不多，時間複雜度會回到：

$$
O(n)
$$

# 二叉搜索樹 get 方法實現

前面我們已經了解了二叉搜索樹（BST）的基本概念，接下來來看它最常見的一個操作：**查找節點**，也就是 `get` 方法。

`get` 方法的作用很簡單：

> 根據給定的 `key`，找到對應節點的 `value`。

如果找不到，就回傳 `null`。

## 一、先定義節點類別

在二叉搜索樹中，每一個節點都需要保存：

* `key`：用來比較大小
* `value`：實際儲存的資料
* `left`：左子節點
* `right`：右子節點

這裡把節點設計成 `BSTTree1` 的靜態內部類：

```java
public class BSTTree1 {
    static class BSTNode {
        int key;
        Object value;
        BSTNode left;
        BSTNode right;

        public BSTNode(int key) {
            this.key = key;
        }

        public BSTNode(int key, Object value) {
            this.key = key;
            this.value = value;
        }

        public BSTNode(int key, Object value, BSTNode left, BSTNode right) {
            this.key = key;
            this.value = value;
            this.left = left;
            this.right = right;
        }
    }
}
```

## 二、二叉搜索樹至少要有根節點

在 BST 類別中，最基本的屬性就是：

```java
BSTNode root;  // 根節點
```

整棵樹的查找、插入、刪除，通常都是從根節點開始。

## 三、get 方法的查找思路

查找時，從根節點開始，把目前節點的 `key` 和目標 `key` 做比較，分成 3 種情況：

### 1. 目標 key 比目前節點小

代表要找的節點應該在**左子樹**，所以往左走。

### 2. 目標 key 比目前節點大

代表要找的節點應該在**右子樹**，所以往右走。

### 3. 兩者相等

表示已經找到目標節點，直接回傳它的 `value`。

這個判斷規則不只適用於根節點，也適用於後續每一個子節點。
因此，這是一個很適合用**遞迴**實作的問題。

## 四、遞迴版本的 get 方法

### 1. 對外公開的方法

`get` 是給外部呼叫的，所以通常會從 `root` 開始查找：

```java
public Object get(int key) {
    return doGet(root, key);
}
```

### 2. 真正執行查找的方法

```java
private Object doGet(BSTNode node, int key) {
    if (node == null) {
        return null; // 沒有找到
    }
    if (key < node.key) {
        return doGet(node.left, key); // 從左子樹繼續查找
    } else if (key > node.key) {
        return doGet(node.right, key); // 從右子樹繼續查找
    } else {
        return node.value; // 找到了，回傳值
    }
}
```

### 如何理解這段遞迴程式？

這段程式其實就是在重複做同一件事：

1. 先看目前節點是不是 `null`

    * 如果是，表示這條路已經走到底了，還是沒找到，所以回傳 `null`

2. 如果目前節點不為 `null`

    * `key < node.key`：往左找
    * `key > node.key`：往右找
    * `key == node.key`：找到答案

也就是說，每次遞迴都在縮小查找範圍。

### 測試資料範例

下面建立了一棵二叉搜索樹：

```java
class TestBSTTree1 {
    public BSTTree1 createTree() {
        BSTTree1.BSTNode n1 = new BSTTree1.BSTNode(1, "张无忌");
        BSTTree1.BSTNode n3 = new BSTTree1.BSTNode(3, "宋青书");
        BSTTree1.BSTNode n2 = new BSTTree1.BSTNode(2, "周芷若", n1, n3);

        BSTTree1.BSTNode n5 = new BSTTree1.BSTNode(5, "说不得");
        BSTTree1.BSTNode n7 = new BSTTree1.BSTNode(7, "殷离");
        BSTTree1.BSTNode n6 = new BSTTree1.BSTNode(6, "赵敏", n5, n7);
        BSTTree1.BSTNode root = new BSTTree1.BSTNode(4, "小昭", n2, n6);

        BSTTree1 tree = new BSTTree1();
        tree.root = root;
        return tree;
    }

    @Test
    void get() {
        BSTTree1 tree = createTree();
        assertEquals("张无忌", tree.get(1));
        assertEquals("周芷若", tree.get(2));
        assertEquals("宋青书", tree.get(3));
        assertEquals("小昭", tree.get(4));
        assertEquals("说不得", tree.get(5));
        assertEquals("赵敏", tree.get(6));
        assertEquals("殷离", tree.get(7));
        assertNull(tree.get(8));
    }
}
```

這棵樹的結構可以理解成：

```text
              4(小昭)
           /           \
     2(周芷若)         6(赵敏)
      /     \          /     \
1(张无忌) 3(宋青书) 5(说不得) 7(殷离)
```

### 查找過程示例

例如執行：

```java
tree.get(5)
```

查找過程如下：

1. 從根節點 `4` 開始

    * `5 > 4`，往右子樹走

2. 到節點 `6`

    * `5 < 6`，往左子樹走

3. 到節點 `5`

    * `5 == 5`，找到，回傳 `"说不得"`

所以 BST 查找並不是一個一個慢慢比，而是每次比較後，直接排除一半方向。

## 五、非遞迴版本的 get 方法

剛剛是用遞迴實現，現在來看**非遞迴**寫法。

思路其實完全一樣，只是把「函式自己呼叫自己」改成「用 while 迴圈不斷往下走」。

```java
public Object get(int key) {
    BSTNode current = root; // 從根節點開始
    while (current != null) {
        if (key < current.key) {
            current = current.left; // 從左子樹繼續查找
        } else if (key > current.key) {
            current = current.right; // 從右子樹繼續查找
        } else {
            return current.value; // 找到了，回傳值
        }
    }
    return null; // 沒有找到
}
```

### 如何理解非遞迴版本？

可以把 `current` 想成「目前正在比較的節點」。

每次迴圈做的事就是：

* 比大小
* 決定往左還是往右
* 更新 `current`
* 持續往下找

如果最後 `current` 變成 `null`，就表示走到底了還是沒找到。

## 六、什麼是尾遞迴？

原文提到一個概念：**尾遞迴**。

尾遞迴的意思是：

> 一個函式在最後一步呼叫自己，而且呼叫完之後不再做其他事情。

例如：

```java
return doGet(node.left, key);
```

或：

```java
return doGet(node.right, key);
```

這就是尾遞迴，因為遞迴呼叫後，沒有額外運算，直接把結果回傳。

也因為這樣，這類程式通常很容易改寫成非遞迴版本。

## 七、完整理解：get 方法的核心本質

BST 的 `get` 方法之所以高效，是因為它利用了二叉搜索樹的性質：

* 左邊都比較小
* 右邊都比較大

所以每次比較之後，都能立刻決定下一步方向，不需要像普通查找那樣把所有節點都掃過一次。

這就是 BST 查找快的原因。

# 二叉搜索樹的泛型版本實現

前面在實作二叉搜索樹時，我們把節點的 `key` 設計成 `int` 類型。

這樣做的原因很簡單：

> 二叉搜索樹中的節點必須能夠比較大小，才能決定往左走還是往右走。

`int` 本身就能直接比較大小，所以一開始用它來當 `key` 很合理。

不過，問題來了：

* 如果我想用 `String` 當 key 呢？
* 如果我想用 `Integer`、`Long`、`LocalDate` 當 key 呢？

也就是說，我們希望二叉搜索樹的 `key` 不只支援 `int`，而是能支援更多可比較的型別。

這時候，就會想到使用 **泛型**。

## 一、先把 key 改成泛型

最直覺的想法，就是先把原本的 `int key` 改成 `T key`：

```java
public class BSTTree1<T> {
    static class BSTNode<T> {
        T key;
        Object value;
        BSTNode<T> left;
        BSTNode<T> right;

        public BSTNode(T key) {
            this.key = key;
        }

        public BSTNode(T key, Object value) {
            this.key = key;
            this.value = value;
        }

        public BSTNode(T key, Object value, BSTNode<T> left, BSTNode<T> right) {
            this.key = key;
            this.value = value;
            this.left = left;
            this.right = right;
        }
    }
}
```

表面上看起來，好像已經完成泛型化了。
但其實這樣還不夠。

## 二、為什麼直接寫 T 還不行？

因為二叉搜索樹的核心前提是：

> `key` 必須能比較大小。

但如果你只是隨便寫一個泛型 `T`，Java 並不能保證這個 `T` 一定可以比較大小。

例如：

* `Integer` 可以比較
* `String` 可以比較
* 但某些自訂類別如果沒有提供比較規則，就不能直接比較

所以，單純寫成 `T` 並不能滿足 BST 的需求。

## 三、限制泛型必須可以比較大小

在 Java 中，如果一個類型想要具備「可比較大小」的能力，通常要實作 `Comparable` 介面。

因此，我們要對泛型加上限制：

```java
T extends Comparable<T>
```

這表示：

> `T` 必須是一個可以和同型別物件比較大小的類型。

改完之後，程式就會變成這樣：

```java
public class BSTTree2<T extends Comparable<T>> {
    static class BSTNode<T extends Comparable<T>> {
        T key;
        Object value;
        BSTNode<T> left;
        BSTNode<T> right;

        public BSTNode(T key) {
            this.key = key;
        }

        public BSTNode(T key, Object value) {
            this.key = key;
            this.value = value;
        }

        public BSTNode(T key, Object value, BSTNode<T> left, BSTNode<T> right) {
            this.key = key;
            this.value = value;
            this.left = left;
            this.right = right;
        }
    }
}
```


## 四、加入根節點

一棵二叉搜索樹，最基本一定要有根節點：

```java
BSTNode<T> root;
```

完整來說：

```java
public class BSTTree2<T extends Comparable<T>> {
    BSTNode<T> root;

    static class BSTNode<T extends Comparable<T>> {
        T key;
        Object value;
        BSTNode<T> left;
        BSTNode<T> right;

        public BSTNode(T key) {
            this.key = key;
        }

        public BSTNode(T key, Object value) {
            this.key = key;
            this.value = value;
        }

        public BSTNode(T key, Object value, BSTNode<T> left, BSTNode<T> right) {
            this.key = key;
            this.value = value;
            this.left = left;
            this.right = right;
        }
    }
}
```

## 五、泛型版本的 get 方法

接著來看查找方法 `get`。

思路和前面 `int` 版本完全一樣，只是原本用 `<`、`>` 比較整數，現在要改用：

```java
key.compareTo(cur.key)
```

來比較大小。

### 非遞迴寫法

```java
public Object get(T key) {
    BSTNode<T> cur = root;
    while (cur != null) {
        int result = key.compareTo(cur.key);
        if (result == 0) {
            return cur.value;
        } else if (result < 0) {
            cur = cur.left;
        } else {
            cur = cur.right;
        }
    }
    return null;
}
```

## 六、如何理解 compareTo 的結果？

`compareTo` 會回傳一個整數，代表比較結果：

* `result == 0`：兩個 key 相等
* `result < 0`：目前查找的 key 比節點 key 小
* `result > 0`：目前查找的 key 比節點 key 大

因此在 BST 中就可以這樣判斷：

* 小於 0，往左子樹走
* 大於 0，往右子樹走
* 等於 0，表示找到節點

## 七、範例：使用 String 作為 key

這裡我們不再用數字當 key，而是改用字串 `"a"`、`"b"`、`"c"` ... 來建立一棵樹。

概念上仍然是同一棵 BST，只是比較規則改成字串的字典順序。

```java
class TestBSTTree2 {
    public BSTTree2<String> createTree() {
        /*
                     d
                   /   \
                  b     f
                 / \   / \
                a   c e   g
         */
        BSTTree2.BSTNode<String> n1 = new BSTTree2.BSTNode<>("a", "张无忌");
        BSTTree2.BSTNode<String> n3 = new BSTTree2.BSTNode<>("c", "宋青书");
        BSTTree2.BSTNode<String> n2 = new BSTTree2.BSTNode<>("b", "周芷若", n1, n3);

        BSTTree2.BSTNode<String> n5 = new BSTTree2.BSTNode<>("e", "说不得");
        BSTTree2.BSTNode<String> n7 = new BSTTree2.BSTNode<>("g", "殷离");
        BSTTree2.BSTNode<String> n6 = new BSTTree2.BSTNode<>("f", "赵敏", n5, n7);
        BSTTree2.BSTNode<String> root = new BSTTree2.BSTNode<>("d", "小昭", n2, n6);

        BSTTree2<String> tree = new BSTTree2<>();
        tree.root = root;
        return tree;
    }

    @Test
    void get() {
        BSTTree2<String> tree = createTree();
        assertEquals("张无忌", tree.get("a"));
        assertEquals("周芷若", tree.get("b"));
        assertEquals("宋青书", tree.get("c"));
        assertEquals("小昭", tree.get("d"));
        assertEquals("说不得", tree.get("e"));
        assertEquals("赵敏", tree.get("f"));
        assertEquals("殷离", tree.get("g"));
        assertNull(tree.get("h"));
    }
}
```

### 這個例子說明了什麼？

它說明了一件事：

> 只要 `key` 所屬的型別實作了 `Comparable`，BST 就能正常工作。

也就是說，BST 並不在乎 `key` 是不是數字，
它只在乎這個 `key` 能不能比較大小。

所以：

* `Integer` 可以
* `String` 可以
* 自訂類別也可以，只要你實作 `Comparable`

## 八、目前還有一個可以再改進的地方

上面的版本雖然把 `key` 改成泛型了，但 `value` 仍然是：

```java
Object value;
```

這代表樹中存放的值雖然可以是任何型別，但每次取出時都只能當成 `Object` 看待，不夠精確，也不夠安全。

所以我們可以進一步把 `value` 也改成泛型。

### 同時對 key 和 value 使用泛型

改進後的版本如下：

```java
public class BSTTree3<K extends Comparable<K>, V> {
    static class BSTNode<K extends Comparable<K>, V> {
        K key;
        V value;
        BSTNode<K, V> left;
        BSTNode<K, V> right;

        public BSTNode(K key) {
            this.key = key;
        }

        public BSTNode(K key, V value) {
            this.key = key;
            this.value = value;
        }

        public BSTNode(K key, V value, BSTNode<K, V> left, BSTNode<K, V> right) {
            this.key = key;
            this.value = value;
            this.left = left;
            this.right = right;
        }
    }
}
```

這樣的設計更完整：

* `K`：表示 key 的型別，必須可比較
* `V`：表示 value 的型別，可以自由指定

例如：

* `BSTTree3<Integer, String>`
* `BSTTree3<String, User>`
* `BSTTree3<LocalDate, Order>`

都可以成立。

## 九、為什麼 `key` 要限制，`value` 不需要？

這是因為在二叉搜索樹中：

* `key` 要負責決定節點放哪裡，所以必須能比較大小
* `value` 只是附加資料，不參與大小比較

因此：

* `key` 必須 `extends Comparable<K>`
* `value` 不需要這個限制

# 二叉搜索樹 min 方法實現

接下來我們來看二叉搜索樹中的 `min` 方法，也就是：

> **找出樹中最小的 key。**

## 一、為什麼最小值一定在最左邊？

先看下面這棵二叉搜索樹：

```text
              4
           /     \
          2       7
         / \      / \
        1   3    6   8
                /
               5
```

根據二叉搜索樹的規則：

* 左子樹的 key 都比目前節點小
* 右子樹的 key 都比目前節點大

因此，**越往左走，key 就越小**。
所以要找最小 key，只要從根節點開始，一直往左走，直到不能再走為止。

例如這棵樹的查找過程：

1. 從 `4` 開始，往左走到 `2`
2. 從 `2` 再往左走到 `1`
3. `1` 已經沒有左子節點了

所以最小 key 就是 `1`。

## 二、核心思路

`min` 方法的邏輯其實很簡單：

* 從根節點開始
* 不斷往左子節點走
* 當某個節點已經沒有左子節點時
* 這個節點就是最小節點

換句話說，判斷「有沒有走到頭」的方法就是：

> 看目前節點的 `left` 是否為 `null`

## 三、遞迴版本實現

### 對外方法

```java
public Object min() {
    return doMin(root);
}
```

### 真正執行查找的方法

```java
public Object doMin(BSTNode node) {
    // 特殊情況：樹是空的
    if (node == null) {
        return null;
    }
    if (node.left == null) { // 已經走到最左邊
        return node.key;
    }
    return doMin(node.left); // 繼續向左找
}
```

### 如何理解這段遞迴？

這段程式每次只做一件事：

1. 先判斷目前節點是不是 `null`

    * 如果是，代表整棵樹是空的，回傳 `null`

2. 如果目前節點不為 `null`

    * 檢查它有沒有左子節點
    * 如果沒有，代表已經到最左邊，直接回傳目前節點的 key
    * 如果有，就繼續往左遞迴查找

你可以把它理解成一句話：

> **一直往左找，直到左邊沒有節點為止。**

### 測試範例

```java
class TestBSTTree1 {
   public BSTTree1 createTree() {
      /*
                  4
                 / \
                2   6
               / \ / \
              1  3 5  7
      */
      BSTTree1.BSTNode n1 = new BSTTree1.BSTNode(1, "张无忌");
      BSTTree1.BSTNode n3 = new BSTTree1.BSTNode(3, "宋青书");
      BSTTree1.BSTNode n2 = new BSTTree1.BSTNode(2, "周芷若", n1, n3);

      BSTTree1.BSTNode n5 = new BSTTree1.BSTNode(5, "说不得");
      BSTTree1.BSTNode n7 = new BSTTree1.BSTNode(7, "殷离");
      BSTTree1.BSTNode n6 = new BSTTree1.BSTNode(6, "赵敏", n5, n7);
      BSTTree1.BSTNode root = new BSTTree1.BSTNode(4, "小昭", n2, n6);

      BSTTree1 tree = new BSTTree1();
      tree.root = root;
      return tree;
   }

   @Test
   public void min() {
      BSTTree1 tree = createTree();
      assertEquals("张无忌", tree.min());
   }
}
```

## 四、非遞迴版本實現

除了遞迴，也可以用迴圈來完成。

```java
public Object min() {
    if (root == null) {
        return null;
    }
    BSTNode current = root;
    while (current.left != null) { // 還沒走到最左邊
        current = current.left;
    }
    return current.key; // 已經走到最左邊
}
```

### 如何理解非遞迴版本？

可以把 `current` 想成「目前所在的節點」。

整個流程就是：

1. 從 `root` 開始
2. 只要左邊還有節點，就繼續往左走
3. 當左邊沒有節點時，停下來
4. 此時 `current` 就是最小節點

這和遞迴版本的邏輯完全相同，只是寫法不同。

# 二叉搜索樹 max 方法實現

接下來我們來看二叉搜索樹中的 `max` 方法，也就是：

> **找出樹中最大的 key。**

## 一、為什麼最大值一定在最右邊？

先看下面這棵二叉搜索樹：

```text
              4
           /     \
          2       7
         / \      / \
        1   3    6   8
                /
               5
```

根據二叉搜索樹的規則：

* 左子樹的 key 都比目前節點小
* 右子樹的 key 都比目前節點大

因此，**越往右走，key 就越大**。
所以要找最大 key，只要從根節點開始，一直往右走，直到不能再走為止。

例如這棵樹的查找過程：

1. 從 `4` 開始，往右走到 `7`
2. 從 `7` 再往右走到 `8`
3. `8` 已經沒有右子節點了

所以最大 key 就是 `8`。

## 二、核心思路

`max` 方法的邏輯和 `min` 方法完全對稱：

* 從根節點開始
* 不斷往右子節點走
* 當某個節點已經沒有右子節點時
* 這個節點就是最大節點

換句話說，判斷「有沒有走到頭」的方法就是：

> 看目前節點的 `right` 是否為 `null`

## 三、遞迴版本實現

### 對外方法

```java
public Object max() {
    return doMax(root);
}
```

### 真正執行查找的方法

```java
private Object doMax(BSTNode node) {
    // 特殊情況：樹是空的
    if (node == null) {
        return null;
    }
    if (node.right == null) { // 已經走到最右邊
        return node.key;
    }
    return doMax(node.right); // 繼續向右找
}
```

### 如何理解這段遞迴？

這段程式每次只做一件事：

1. 先判斷目前節點是不是 `null`

    * 如果是，代表整棵樹是空的，回傳 `null`

2. 如果目前節點不為 `null`

    * 檢查它有沒有右子節點
    * 如果沒有，代表已經到最右邊，直接回傳目前節點的 key
    * 如果有，就繼續往右遞迴查找

你可以把它理解成一句話：

> **一直往右找，直到右邊沒有節點為止。**

### 測試範例

```java
class TestBSTTree1 {
   public BSTTree1 createTree() {
      /*
                  4
                 / \
                2   6
               / \ / \
              1  3 5  7
      */
      BSTTree1.BSTNode n1 = new BSTTree1.BSTNode(1, "张无忌");
      BSTTree1.BSTNode n3 = new BSTTree1.BSTNode(3, "宋青书");
      BSTTree1.BSTNode n2 = new BSTTree1.BSTNode(2, "周芷若", n1, n3);

      BSTTree1.BSTNode n5 = new BSTTree1.BSTNode(5, "说不得");
      BSTTree1.BSTNode n7 = new BSTTree1.BSTNode(7, "殷离");
      BSTTree1.BSTNode n6 = new BSTTree1.BSTNode(6, "赵敏", n5, n7);
      BSTTree1.BSTNode root = new BSTTree1.BSTNode(4, "小昭", n2, n6);

      BSTTree1 tree = new BSTTree1();
      tree.root = root;
      return tree;
   }

   @Test
   public void max() {
      BSTTree1 tree = createTree();
     assertEquals("殷离", tree.max());
   }
}
```

## 四、非遞迴版本實現

除了遞迴，也可以用迴圈來完成：

```java
public Object max() {
    if (root == null) {
        return null;
    }
    BSTNode current = root;
    while (current.right != null) { // 還沒走到最右邊
        current = current.right;
    }
    return current.key; // 已經走到最右邊
}
```

### 如何理解非遞迴版本？

可以把 `current` 想成「目前所在的節點」。

整個流程就是：

1. 從 `root` 開始
2. 只要右邊還有節點，就繼續往右走
3. 當右邊沒有節點時，停下來
4. 此時 `current` 就是最大節點

這和遞迴版本的邏輯完全相同，只是寫法不同。

# 二叉搜索樹 put 方法實現

接下來我們來看二叉搜索樹中的 `put` 方法，也就是：

> **向二叉搜索樹中新增節點；如果 key 已存在，則更新該節點的 value。**

## 一、put 方法要處理哪兩種情況？

`put` 方法的核心只有兩種情況：

### 1. key 已存在

如果樹中已經有這個 key，就不需要新增節點，只要把原本節點的 `value` 更新掉即可。

### 2. key 不存在

如果整棵樹都找不到這個 key，就要建立一個新節點，並把它插入到正確的位置。

## 二、先看一棵範例樹

```text
              4
           /     \
          2       7
         / \      / \
        1   3    6   8
                /
               5
```

### 情況一：key 已存在，更新 value

假設現在要新增：

```java
key = 1
```

查找過程如下：

1. 先和根節點 `4` 比較
   因為 `1 < 4`，所以往左子樹走

2. 再和節點 `2` 比較
   因為 `1 < 2`，所以繼續往左子樹走

3. 最後找到節點 `1`

這表示：

* `1` 這個 key 已經存在
* 不需要建立新節點
* 只要更新它的 `value` 即可

### 情況二：key 不存在，新增節點

假設現在要新增：

```java
key = 9
```

查找過程如下：

1. 和 `4` 比較
   因為 `9 > 4`，往右走

2. 和 `7` 比較
   因為 `9 > 7`，繼續往右走

3. 和 `8` 比較
   因為 `9 > 8`，再往右走

4. 發現 `8` 的右子節點是 `null`

這代表什麼？

這代表我們已經走到頭了，整棵樹都沒有找到 `9`，所以要建立一個新的節點，並把它放到 `8` 的右子樹位置。

插入後的結果如下：

```text
              4
           /     \
          2       7
         / \      / \
        1   3    6   8
                /     \
               5       9
```

## 三、put 的基本查找思路

其實 `put` 的前半段，和 `get` 很像：

* 從根節點開始
* 比較 `key` 和目前節點的大小
* 小於就往左走
* 大於就往右走
* 等於就更新 value

先看基本骨架：

```java
public void put(int key, Object value) {
    BSTNode current = root; // 從根節點開始
    while (current != null) {
        if (key < current.key) {
            current = current.left; // 繼續往左子樹走
        } else if (key > current.key) {
            current = current.right; // 繼續往右子樹走
        } else {
            current.value = value; // key 已存在，更新 value
            return;
        }
    }
    // key 不存在，建立新節點並插入
    BSTNode newNode = new BSTNode(key, value);
}
```

## 四、為什麼這樣還不夠？

上面的程式雖然能找到：

* key 是否存在
* 或者最後走到 `null`

但有一個問題：

> **如果要插入新節點，我們必須知道它要接在誰的下面。**

例如插入 `9` 時，最後雖然知道應該插入，但還必須知道：

* `9` 的父節點是 `8`

因為只有找到父節點，才能把新節點掛到父節點的左邊或右邊。

## 五、如何記住父節點？

觀察插入 `9` 時的移動路徑：

```text
4 -> 7 -> 8 -> null
```

當 `current` 最後變成 `null` 前，
它其實曾經停在 `8`。

所以我們只要在每次移動 `current` 前，先把目前節點記錄下來，就能保留父節點資訊。

這樣就可以多加一個變數：

```java
BSTNode parent = null;
```

### 加入 parent 後的寫法

```java
public void put(int key, Object value) {
    BSTNode current = root; // 從根節點開始
    BSTNode parent = null;  // 用來記錄父節點

    while (current != null) {
        parent = current; // 先記住目前節點

        if (key < current.key) {
            current = current.left; // 繼續往左走
        } else if (key > current.key) {
            current = current.right; // 繼續往右走
        } else {
            current.value = value; // key 已存在，直接更新
            return;
        }
    }

    // key 不存在，建立新節點
    BSTNode newNode = new BSTNode(key, value);
}
```

這時候：

* `current == null` 代表找不到 key
* `parent` 就是新節點要接上的父節點


## 六、找到父節點後，怎麼插入新節點？

接下來只要判斷：

* 新節點的 key 比父節點小，掛到左邊
* 新節點的 key 比父節點大，掛到右邊

另外還有一個特殊情況：

* 如果 `parent == null`
* 表示原本整棵樹是空的
* 這時新節點就是第一個節點，要直接設成 `root`

完整程式如下：

```java
public void put(int key, Object value) {
    BSTNode current = root; // 從根節點開始
    BSTNode parent = null;  // 記錄父節點

    while (current != null) {
        parent = current;

        if (key < current.key) {
            current = current.left;
        } else if (key > current.key) {
            current = current.right;
        } else {
            current.value = value; // key 已存在，更新 value
            return;
        }
    }

    // key 不存在，建立新節點
    BSTNode newNode = new BSTNode(key, value);

    if (parent == null) {
        root = newNode; // 原本是空樹
    } else if (key < parent.key) {
        parent.left = newNode; // 插入到父節點左邊
    } else {
        parent.right = newNode; // 插入到父節點右邊
    }
}
```
## 七、測試程式

```java
class TestBSTTree1 {
    public BSTTree1 createTree() {
        /*
                     4
                   /   \
                  2     6
                 / \   / \
                1   3 5   7
         */
        BSTTree1.BSTNode n1 = new BSTTree1.BSTNode(1, "张无忌");
        BSTTree1.BSTNode n3 = new BSTTree1.BSTNode(3, "宋青书");
        BSTTree1.BSTNode n2 = new BSTTree1.BSTNode(2, "周芷若", n1, n3);

        BSTTree1.BSTNode n5 = new BSTTree1.BSTNode(5, "说不得");
        BSTTree1.BSTNode n7 = new BSTTree1.BSTNode(7, "殷离");
        BSTTree1.BSTNode n6 = new BSTTree1.BSTNode(6, "赵敏", n5, n7);
        BSTTree1.BSTNode root = new BSTTree1.BSTNode(4, "小昭", n2, n6);

        BSTTree1 tree = new BSTTree1();
        tree.root = root;
        return tree;
    }

    @Test
    public void put() {
        BSTTree1 tree = new BSTTree1();
        tree.put(4, new Object());
        tree.put(2, new Object());
        tree.put(6, new Object());
        tree.put(1, new Object());
        tree.put(3, new Object());
        tree.put(7, new Object());
        tree.put(5, new Object());

        assertTrue(isSameTree(createTree().root, tree.root));

        tree.put(1, "教主张无忌");
        assertEquals("教主张无忌", tree.get(1));
    }
}
```